#include<stdio.h>
#include<float.h>
#include<limits.h>

int main(void) {
	int in;
	unsigned int unit;

	float fl;
	double db;

	char ch = 'A'
		char str[10] = "abc"

	printf(" int 크기 : %d byte \n", sizeof(in));
	printf(" int 범위 : %d ~ %d \n", INT_MIN, INT_MAX);
	printf(" int 표현 : %d byte \n", sizeof(in));




	return 0;
}

